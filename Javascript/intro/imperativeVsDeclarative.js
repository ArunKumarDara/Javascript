// imperative way of coding --> step by step process
// declarative way of coding --> i want this thing to get done

let arr = [1, 2, 3, 4, 5];

let arr1 = arr.map((each) => each * each);

console.log(arr1);
